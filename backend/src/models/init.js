/**
 * Run schema.sql in your Supabase project:
 *   1. Go to https://supabase.com/dashboard → your project → SQL Editor
 *   2. Paste the contents of src/models/schema.sql and click Run
 *
 * The schema creates all 8 tables with proper indexes and ENUMs.
 * Supabase handles uuid_generate_v4() via the pgcrypto extension automatically.
 */
console.log('Run src/models/schema.sql in the Supabase SQL Editor to initialize the database.');
